# ─────────────────────────────────────────────────────────
#  민감 정보 — 절대 커밋 금지 (한 번 커밋되면 히스토리에 영구히 남음)
# ─────────────────────────────────────────────────────────
# DB 연결문자열·비밀번호
db_url.txt
.db_url

# 환경변수: 실제 값 파일은 무시, 템플릿(.env.example)만 커밋
.env
.env.*
!.env.example

# 서비스 계정·API 키·인증서
*service-account*.json
*service_account*.json
mystic-pier*.json
*-key.json
*credentials*.json
*secret*.json
*.pem
*.key
secrets/

# 🔴 Google 서비스계정 키 폴더 (실측: key/mystic-pier-...json)
key/
*.json.key

# ─────────────────────────────────────────────────────────
#  의존성·빌드 산출물 (용량 큼 / 재생성 가능)
# ─────────────────────────────────────────────────────────
node_modules/
dist/
build/
.vite/

# ─────────────────────────────────────────────────────────
#  Python
# ─────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# ─────────────────────────────────────────────────────────
#  OS / 에디터
# ─────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/

# ─────────────────────────────────────────────────────────
#  대용량 생성물 (스크립트로 재생성 가능 → 커밋 제외)
# ─────────────────────────────────────────────────────────
리서치/도구/n1_skew/corpus/
# 토모 표정 원본 고해상도(최적화본만 커밋, 원본은 로컬 보존)
개발/프론트엔드_무스부/tomori-app/public/images/tomo/_원본_고해상도/

# Claude Code 로컬 설정 (머신별)
.claude/settings.local.json
